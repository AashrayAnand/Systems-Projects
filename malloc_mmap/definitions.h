#ifndef DEFINITIONS
#define DEFINITIONS

#include <stdio.h>
#include <sys/types.h> /* Type definitions used by many programs */
#include <unistd.h>
#include <assert.h> /* for testing if memory is being allocated correctly */
#include <pthread.h> /* needed for thread-safe memory allocation */
#include <string.h>

#endif
